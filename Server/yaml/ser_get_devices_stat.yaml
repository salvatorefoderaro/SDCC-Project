kind: Service
apiVersion: v1
metadata:
  name: getdevicesstatservice
spec:
  selector:
    app: getdevicesstat
  ports:
  - protocol: "TCP"
    # Port accessible inside cluster
    port: 8020
    # Port to forward to inside the pod
    targetPort: 8020
  type: NodePort